{
  "name": "12-desafio-piedra-papel-tijeras",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url /piedra-papel-o-tijeras-vs-CPU",
    "deploy": "gh-pages -d build -b gh-pages",
    "build-and-deploy": "pnpm build && pnpm deploy",
    "clean-build": "rm -rf build .parcel-cache node_modules/.cache && parcel build src/index.html --dist-dir build --public-url /piedra-papel-o-tijeras-vs-CPU"
  },
  "dependencies": {
    "12-desafio-piedra-papel-tijeras": "link:",
    "gh-pages": "^6.3.0",
    "parcel": "^2.16.3"
  }
}
