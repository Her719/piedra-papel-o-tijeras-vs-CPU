{
  "name": "4-todolist",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "gh-pages -d ./dist",
    "build-and-deploy": "pnpm build && pnpm deploy",
    "clean-build": "rm -rf dist .parcel-cache node_modules/.cache && parcel build index.html"
  },
  "dependencies": {
    "parcel": "^2.16.3",
    "gh-pages": "^6.3.0"
  }
}
